pipeline {
	agent any
	states {
		stage('Init') {
			steps {
				echo 'Hi, this is David'
				echo 'Let's start the test'
			}
		}
		stage('Build'){
			steps{
				echo 'We are building the project'
			}
		}
		stage('Deploy'){
			steps{
				echo 'We are deploying the project'
			}
		}
	}
}
